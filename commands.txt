pwd -- print working directory > displays your current working directory location
cd [loc] -- change directory > allows you change directory
mkdir -- make directory >create new folder 
ls -- list files and folders 
touch [filename] -- create new file 
rm [filename] > deleting files
----
Note: repository is like a folder/container that stores files/folders related project
it keeps tracks of changes made over time
> local repository -- version of the repoisitory is stored on local machine
>remote reposit -- verson of repositoryis stored on a server or cloud platform (Github)

git commands:

git init --initialize a folder/local repo
git config --global user.name "username"
git config --global user.email "email address" 
git config --global user.name
git config --global user.name
git add . --adds all modified files in your current directory to the staging area 
git add [filename] --add specific file in your current directory to the staging area
git commit -m "message' -saves the changes tat you made to the files in the staging area to the repository with a message.
git status --displays the current status of your files or folder 
git log --displays all the commits that were made
git log --oneline --condense of git log
git push --uploading files/folders from local repository to remote repository
git pull --retrives changes from a repository and merges them into your local repository 
git clone --creates a copy of a remote rpository on your local machine 

